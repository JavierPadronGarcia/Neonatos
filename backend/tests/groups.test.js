const groupController = require('../controllers/group.controller');

describe('Sum', () => {
  test('devuelve 2 cuando se suma 1 + 1', () => {
    const result = 1 + 1;
    expect(result).toBe(2);
  });
});